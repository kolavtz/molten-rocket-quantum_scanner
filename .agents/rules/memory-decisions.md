# Memory – Decisions

Format: [DATE] Area – Decision – Rationale

- [2026-03-19] QuantumShield – All deletions are soft deletes with a Recycle Bin, never load deleted rows into normal queries – preserves auditability and safety.
- [YYYY-MM-DD] …
- [2026-03-20] Database bootstrap – Canonical schema init lives in `src/database.py`; `src/db.py` must not drop/recreate metadata – prevents destructive drift that removed `scans.report_json`/`scan_id` and broke FKs.
- [2026-03-20] Dashboard resilience – Asset and scan view builders must tolerate mixed legacy row shapes (`target` vs `name`) and mixed scan feeds (DB + in-memory) – keeps routes stable during migrations and DB outages.
- [2026-03-20] Table UX – For interactive dashboard tables, prefer client-side sort/search/pagination and return full row sets from route handlers – prevents full-page reloads and avoids sorting only a server page slice.
- [2026-03-20] Scheduler overlap policy – If an automated inventory sweep starts while another scan is running, log and skip that cycle (status=in_progress) instead of treating as an error.
- [2026-03-20] Logging portability – Use ASCII-only status text in scheduler logs to avoid Windows cp1252 encoding failures in console handlers.
- [2026-03-20] Scheduler lifecycle – Do not start scheduler during module import; start only in runtime entrypoint to avoid duplicate threads/reloader side-effects and perceived startup stalls.
- [2026-03-20] DB startup resilience – Keep MySQL probe fast-fail by default (short timeout/retries) and make values env-configurable so app can fall back to JSON mode quickly when DB is unreachable.
- [2026-03-20] Inventory scan orchestration – Inventory routes must inject the scan pipeline callable via app config instead of importing `web.app` from service code – avoids duplicate module initialization and scan flow breakage under different run modes.
- [2026-03-20] Inventory UX telemetry – Bulk inventory scans must expose per-asset progress (`current/total/target/success/fail`) through status polling for real-time operator feedback.
- [2026-03-20] Flask testing mode â€“ Route handlers and graph/report builders should skip MySQL reads/writes when `app.config["TESTING"]` is true â€“ keeps pytest deterministic when the local database is unreachable.
- [2026-03-20] Discovery graph API â€“ `/api/discovery-graph` should return `nodes` and `edges` at the top level rather than a nested `data` object â€“ matches the current client/test contract.
- [2026-03-20] App bootstrap â€“ Defer MySQL initialization until runtime entrypoints instead of module import â€“ keeps `web.app` import fast and avoids startup hangs in tests.
- [2026-03-21] Dashboard route unification – `/dashboard/assets` is the single canonical dashboard route and must combine aggregate KPIs with inventory asset rows in one handler – prevents empty dashboard tables caused by relying on aggregate payload `scans` lists that can be blank.
- [2026-03-21] API consolidation – Prefer `/api/dashboard` as the unified action endpoint for dashboard data and scan orchestration (`action` payload contract) while keeping legacy endpoints as compatibility wrappers – reduces client-side endpoint sprawl and keeps scan/dashboard behaviors synchronized.
- [2026-03-21] CBOM metrics â€“ Compute certificate issue counts from active inventory joins plus CBOM summary rows, and surface a multi-cell heatmap instead of a placeholder summary â€“ keeps the CBOM view tied to live MySQL telemetry.
- [2026-03-21] Theme mapping â€“ Keep theme config values normalized and mirror them into both the new CSS vars and the legacy `--accent-primary`/`--bg-dark` aliases â€“ avoids breaking older templates while centralizing color control.
- [2026-03-21] Reporting PDFs â€“ In non-test runs, build report sections from live DB services instead of the scan cache â€“ keeps scheduled and on-demand reports aligned with current inventory, CBOM, and PQC data.
- [2026-03-21] Report dispatch â€“ Use the audit log as the dedupe source for scheduled report sends so cron-style runs stay idempotent without adding a new table â€“ keeps scheduler behavior simple and MySQL-backed.
- [2026-03-21] Schedule passwords â€“ Store per-schedule PDF passwords encrypted in `report_schedules` and redact them from public listing endpoints â€“ lets the scheduler send protected PDFs without exposing the secret back to users.
- [2026-03-21] Table modes â€“ Split table handling into server-managed and client-managed modes; server-paginated pages use explicit sort links while plain tables get the local JS enhancer â€“ avoids duplicate toolbars and keeps MySQL-backed pages predictable.
- [2026-03-21] Theme palettes â€“ Store separate dark/light palettes in `theme.json` and normalize each palette independently â€“ gives admin control over both modes without breaking contrast on startup or toggle.
- [2026-03-21] Shared table helper â€“ `paginate_query()` should accept both SQLAlchemy queries and enriched in-memory row sequences â€“ lets the same pagination/search/sort contract power `/assets` and future tables without duplicating query logic.
- [2026-03-21] KPI freshness â€“ Invalidate dashboard caches on asset create/edit/delete and scan completion â€“ keeps home and inventory KPI cards synchronized with MySQL immediately after mutations.
- [2026-03-21] Inventory template source of truth â€“ Use `web/templates/asset_inventory.html` as the live inventory page and keep `web/templates/assets.html` as an alias â€“ prevents drift between the active page and the alternate template name.
- [2026-03-22] Asset inventory bulk actions â€“ Scope selection state to a shared `data-table-shell` wrapper and keep row checkbox `form` attributes as a native fallback â€“ fixes the zero-count display and keeps bulk delete/edit working even if page-specific JS is bypassed.
- [2026-03-22] Asset inventory visibility â€“ Surface the table-scoped active count in the inventory header and apply a stricter soft-delete filter (`is_deleted=False` plus `deleted_at IS NULL`) â€“ prevents deleted rows from leaking into the live inventory and makes the KPI match the current view.
- [2026-03-22] Inventory mutations â€“ Asset inventory edit/delete actions should call JSON API endpoints from the UI, with HTML form routes retained only as fallback/compatibility paths â€“ keeps the page behavior truly data-backed instead of relying on form-only UI flows.
- [2026-03-22] Soft-delete audit compatibility â€“ When persisting `deleted_by_user_id`, detect legacy numeric DB schemas at runtime and store non-numeric user IDs as NULL instead of forcing UUID strings â€“ prevents MySQL DataError (1265) during bulk delete in mixed-schema deployments.
- [2026-03-22] Inventory delete UX â€“ API delete responses should include `deleted_ids` and the UI should remove table rows client-side without page reload â€“ provides immediate feedback and avoids disruptive refresh loops on active inventory pages.
- [2026-03-22] Non-inventory mutation pattern â€“ Discovery/recycle-bin table actions should use JSON API submissions with in-place DOM updates while retaining form fallback â€“ extends fast no-refresh behavior beyond inventory and keeps mutation UX consistent.
- [2026-03-22] Cascade parity â€“ Legacy dashboard/recycle handlers must delegate to or mirror inventory cascade rules (including `DiscoveryItem`, `ComplianceScore`, `CyberRating`, scan-linked children) â€“ prevents restore/delete drift and orphaned telemetry rows.
- [2026-03-22] Inventory live-refresh compatibility â€“ Client-side table polling must normalize both API field shapes (`asset_name/type` and `name/asset_type`) and update only mutable cells while preserving checkbox/action DOM state â€“ avoids broken or blank table updates across mixed API routes.
- [2026-03-22] Asset action wiring â€“ Prefer data-attribute action hooks (`data-open-asset-scan` / `data-open-asset-details`) over inline onclick handlers for inventory row actions â€“ keeps handlers re-bindable after DOM updates and reduces HTML/JS coupling.
- [2026-03-22] Skill source-to-runtime setup â€“ When a full upstream skill repo is cloned under `.agents/skills/<repo-name>/`, promote the actual runtime skill from `.claude/skills/<skill-name>/` into `.agents/skills/<skill-name>/` (with `SKILL.md`, `scripts/`, `data/`) for direct workspace invocation.
- [2026-03-22] Agent skill install pattern â€“ Install external skills under `.agents/skills/<skill-name>/` with `SKILL.md` plus required runtime assets (`scripts/`, `data/`) so local agent workflows and optional script-powered lookups both function consistently.
- [2026-03-22] Canonical schema restore â€“ `schema_v2_inventory_api_first.sql` must drop canonical + legacy drift tables/views (including old `scan_id` dependents like `asset_dns_records` and `cbom_reports`) before recreate â€“ prevents FK/import failures on mixed historical schemas.
- [2026-03-22] Inventory API test path â€“ Asset inventory routes should bypass the `testing_mode=True` empty-data shortcut and still read live SQL-backed inventory rows â€“ keeps the `Frontend -> API -> SQL -> Response -> Frontend` contract intact under Flask tests and API-first flows.
- [2026-03-22] Scan schema bridging â€“ The `Scan` ORM must populate both compatibility columns (`scan_id`, `target`) and canonical columns (`scan_uid`, `requested_target`, `normalized_target`, `scan_kind`) before save â€“ prevents mixed-schema MySQL installs from rejecting inserts while keeping existing route/query code stable.
- [2026-03-23] Mixed-schema dashboard APIs â€“ `/api/discovery` must read the live split discovery tables (`discovery_domains`, `discovery_ips`, `discovery_software`, `discovery_ssl`) and `/api/cyber-rating` should derive rows from `CyberReportingService` rather than the legacy `CyberRating.organization_id` query path â€“ prevents broken dashboard APIs on the current MySQL schema.
- [2026-03-23] Scan ingestion richness â€“ The scan pipeline should persist normalized certificate identity/validity/key metadata plus split discovery rows, and asset detail APIs should surface that richer telemetry back to the inventory UI â€“ keeps certificate, CBOM, PQC, and discovery dashboards fed from the same end-to-end SQL-backed workflow.
